# Base URL of the Spring Boot API. Copy to .env (git-ignored) and adjust per environment.
# Without it the client uses http://localhost:8080. The Docker image sets it at build time.
VITE_API_BASE_URL=http://localhost:8080
