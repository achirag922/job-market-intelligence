# Included only by the HTTPS server (nginx-https.conf). One year, subdomains included. Add
# "preload" only after submitting the domain to the browsers' preload list.
add_header Strict-Transport-Security "max-age=31536000; includeSubDomains" always;
